---
const steps = [
  {
    num: "01",
    title: "Analisi",
    desc: "Studio approfondito del mercato, competitor e opportunità locali e nazionali.",
  },
  {
    num: "02",
    title: "Strategia",
    desc: "Definizione del posizionamento unico e piano d'azione basato su dati reali.",
  },
  {
    num: "03",
    title: "Sviluppo",
    desc: "Creazione dell'ecosistema digitale con le migliori tecnologie disponibili.",
  },
  {
    num: "04",
    title: "Dominio",
    desc: "Ottimizzazione continua per dominare ricerche nazionali e locali nel tempo.",
  },
];

const whys = [
  "Siti web su misura, zero template standard. Non ti vendo il sito vetrina pre-confezionato. Progetto da zero un ecosistema digitale cucito sulle reali esigenze della tua attività, ottimizzato per convertire i visitatori in clienti",
  "Pronto per il futuro delle ricerche. Il web sta cambiando: non basta più solo Google. Ottimizzo la tua presenza per fare in modo che la tua attività venga consigliata da ChatGPT, Gemini e assistenti vocali prima dei tuoi concorrenti.",
  "Controllo totale del tuo mercato in zona. Studio i punti deboli dei tuoi concorrenti locali per posizionare il tuo brand come l'unica scelta ovvia per chi cerca i tuoi servizi in città",
  "Reputazione e brand sotto controllo. Nessun assistente diverso ogni mese. Curo personalmente ogni dettaglio della tua immagine online (foto, recensioni e social) per assicurarmi che il tuo brand sia sempre impeccabile e generi fiducia.",
];
---

<section class="section" id="metodo">
  <div class="container">

    <div class="section-label text-center">
    </div>
    <div class="text-center" style="margin-bottom: 16px;">
      <h2 class="section-title">Il Metodo</h2>
    </div>
    <p class="section-subtitle text-center" style="margin-bottom: 72px;">
      Un processo per trasformare la tua presenza digitale in un vantaggio competitivo e portare clienti a comprare.
    </p>

    <div class="method-layout">
      <!-- Steps -->
      <div class="steps-grid">
        {steps.map((s, i) => (
          <div class="step-card" style={`animation-delay: ${i * 0.1}s`}>
            <div class="step-num">{s.num}</div>
            <h3 class="step-title">{s.title}</h3>
            <p class="step-desc">{s.desc}</p>
          </div>
        ))}
      </div>

      <!-- Why -->
      <div class="why-panel">
        <h3 class="why-title">Perché scegliere un servizio sartoriale (e non un'agenzia standard)?</h3>
        <ul class="why-list">
          {whys.map(w => (
            <li class="why-item">
              <span class="why-icon">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>
              </span>
              <span>{w}</span>
            </li>
          ))}
        </ul>
      </div>
    </div>
  </div>
</section>

<style>
  .method-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: start;
  }

  .steps-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }

  .step-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px 24px;
    transition: all 0.25s ease;
    position: relative;
    overflow: hidden;
  }

  .step-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(59,130,246,0.04) 0%, transparent 60%);
    pointer-events: none;
  }

  .step-card:hover {
    border-color: rgba(59,130,246,0.2);
    background: var(--bg-card-hover);
    transform: translateY(-2px);
  }

  .step-num {
    font-family: var(--font-display);
    font-size: 44px;
    font-weight: 800;
    color: rgba(59,130,246,0.2);
    line-height: 1;
    margin-bottom: 12px;
    letter-spacing: -0.04em;
  }

  .step-title {
    font-family: var(--font-display);
    font-size: 20px;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 8px;
  }

  .step-desc {
    font-size: 14px;
    color: var(--text-muted);
    line-height: 1.6;
  }

  /* Why panel */
  .why-panel {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 40px;
  }

  .why-title {
    font-family: var(--font-display);
    font-size: 22px;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 28px;
  }

  .why-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .why-item {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    font-size: 15px;
    color: var(--text-muted);
    line-height: 1.5;
  }

  .why-icon {
    width: 22px;
    height: 22px;
    background: rgba(59,130,246,0.15);
    color: var(--blue);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    margin-top: 1px;
  }

  @media (max-width: 960px) {
    .method-layout { grid-template-columns: 1fr; }
  }

  @media (max-width: 480px) {
    .steps-grid { grid-template-columns: 1fr; }
  }
</style>
